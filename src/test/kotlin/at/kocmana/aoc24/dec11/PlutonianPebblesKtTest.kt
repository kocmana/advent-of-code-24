package at.kocmana.aoc24.dec11

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

class PlutonianPebblesKtTest {
/*
    @Test
    fun testCalculateNextStone() {
        assertEquals(listOf(1L), calculateNextStone(0))
        assertEquals(listOf(12L, 34), calculateNextStone(1234))
        assertEquals(listOf(3L * 2024), calculateNextStone(3))
    }

    @Test
    fun calculateNextBlink() {
        val input = listOf(125L, 17)
        val expected = listOf(253000L, 1, 7)

        val actual = calculateNextBlink(input)

        assertEquals(expected, actual)
    }

    @Test
    fun calculate6Blinks() {
        val input = listOf(125L, 17L)
        val expected = listOf(2097446912L, 14168, 4048, 2, 0, 2, 4, 40, 48, 2024, 40, 48, 80, 96, 2, 8, 6, 7, 6, 0, 3, 2)

        val actual = generateSequence(input) { calculateNextBlink(it) }
            .take(7)
            .last()
            .toList()

        assertEquals(expected, actual)
    }
*/
}
